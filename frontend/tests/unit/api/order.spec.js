// import OrderMock from '@/api/order'
// import request from '@/utils/request'
//
// const getOrderMock = (success = true) => {
//     request.fetch = success ? jest.fn().mockResolvedValue('get data') : jest.fn().mockRejectedValue('error')
// }
//
// describe.skip('Tests for Order Api', () => {
//     const id = 1
//
//     it('Should return a promise when - "getOrders" is called and api call succeeds', async () => {
//         getOrderMock()
//         const response = await OrderMock.getOrders(id)
//         expect(response).toEqual('get data')
//     })
//
//     it('Should return a promise when - "getOrders" is called and api fails', async () => {
//         getOrderMock(false)
//         try {
//             await OrderMock.getOrders(id)
//         } catch (e) {
//             expect(e).toEqual('error')
//         }
//     })
// })
it('pass please', () => {

})
